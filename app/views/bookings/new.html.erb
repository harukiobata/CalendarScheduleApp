<turbo-frame id="booking_frame">
  <%= render partial: "bookings/steps", locals: { current_step: @current_step } %>
  <%= render partial: "bookings/form", locals: { booking: @booking, owner: @owner } if defined?(@booking) %>
</turbo-frame>
