<h3>使えるコード一覧</h3>
<ul>
  <li><code>&lt;%= @booking.name %&gt;</code> → 予約者の名前</li>
  <li><code>&lt;%= @booking.email %&gt;</code> → 予約者のメールアドレス</li>
  <li><code>&lt;%= @booking.start_time %&gt;</code> → 予約開始日時</li>
  <li><code>&lt;%= @booking.end_time %&gt;</code> → 予約終了日時</li>
  <li><code>&lt;%= @booking.memo %&gt;</code> → メモ</li>
</ul>